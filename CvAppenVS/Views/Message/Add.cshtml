@using CvAppen.Web.DTOs
@model SendMessageDto

<a class="nav-link d-flex flex-column align-items-center">
<form asp-action="add" method="post">
    <input asp-for="ToUserId" type="Hidden" />
    <div>
        <label for="Text"> Meddelande: </label>
        <textarea asp-for="Text" rows="4" cols = "50"></textarea>
         </div>
        @if(!User.Identity.IsAuthenticated)
            {
                <div><label for="SenderName"> Ditt namn: 
                    <input asp-for = "SenderName" type="Text" />
                </label></div>
            }
       <button type="submit">Skicka</button>
    </form>
    </a>
