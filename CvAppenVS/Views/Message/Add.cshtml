@model CvAppenVS.Models.DTOs.SendMessageDto

<form asp-for="add" method="post">
    <input asp-for="ToUserId" type="Hidden" />
    <div>
        <label for="Text"> Meddelande: </label>
        <textarea asp-for="Text" rows="4" columns = "50""></textarea>
         </div>
        @if(!User.Identity.IsAuthenticated)
            {
                <div><label for="SenderName"> Ditt namn: 
                    <input asp-for = "SenderName" type="Text" />
                </label></div>
            }
       <button type="submit">Skicka</button>
    </form>
